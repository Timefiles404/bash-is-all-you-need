# Generated by web/tools/build.py. Everything here is reproducible from the
# repository plus a Go toolchain, and none of it should be reviewed as source.

# `go mod vendor` output for the shell's module, patched in place by jspipe.py.
# 9.2 MB of somebody else's code, regenerated on every build.
tools/wasmshell/vendor/

# Compiled WebAssembly and the manifest that records which Go built it.
assets/wasm/

# Per-level output: assembled sources, build tables, artifacts.
assets/levels/

# The vendored Go host support file is copied from GOROOT by build.py, and the
# version stamp beside it has to match the .wasm. Both are generated; both are
# checked in anyway, because a reader cloning this repository should be able to
# serve the site without a Go toolchain if the assets are also present.
!assets/js/runtime/vendor/
