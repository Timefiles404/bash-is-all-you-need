# AGENTS.md

这个仓库的约定。阶段 05 的 Agent 启动时会读这个文件，把它塞进自己的
系统提示词——所以它既是文档，也是这个功能本身的活演示，见
[docs/05-live-forever.md](docs/05-live-forever.md)。

## 这个仓库是什么

一门循序渐进的课。`stages/` 下的每个目录，都是 Agent 在课程某一点上的
**完整、独立的快照**。目录之间的重复是故意留的：读者应该能直接
`diff stages/04-the-cache stages/05-live-forever`，一眼看出这一步
只多了一个想法。

**不要把各阶段的共享代码重构成公共包。** 这是唯一一个会毁掉这个仓库
存在意义的改动。

这门课分两半。**第一部分（00–08）是仪表盘；第二部分（09 起）是
生产环境里真正会坏掉的东西。** 第二部分是从**阶段 07** 分出去的，不是
阶段 08：阶段 08 是全仓库唯一带依赖的阶段，而且明说了是可选的，把它
一路带下主干就等于让它变成必需。

这条规矩说的是第二部分从哪里**开始**，不是它里面每个阶段都从哪里来。
阶段 09 是从 07 复制的；阶段 10 是从 09 复制的，往后依此类推——因为还有
一条同样要紧的性质：`diff stages/NN stages/NN+1` 只能看到**一个**想法。
每次都从 07 复制，会把前面所有第二部分的想法一并塞进那个 diff。所以：
`cp -r stages/<上一个> stages/NN-name`，然后只加那一个想法——`<上一个>`
对阶段 09 来说是阶段 07，此后就是紧挨着的前一个阶段。

## 规则

- **不许有依赖**：只有标准库和 `golang.org/x/sys`，后者钉死在 v0.41.0，
  因为 v0.42+ 声明了 `go 1.25.0`。没有 SDK，没有 TUI 框架，没有 JSON 库，
  没有测试框架。
  阶段 08 是唯一的例外（`mvdan.cc/sh/v3` 钉在 v3.12.0，配上 v0.33.0 的
  `golang.org/x/term`，把模块的底线守在 go 1.24.0）。加任何东西之前，
  先读它的 `go.mod`——依赖的 `go` 指令也是它代价的一部分，而且是
  没人会主动告诉你的那部分。
- **每一条教学论断都必须有 `docs/wire-notes.md` 撑着**，那份文件逐字节
  记下了一个真实网关到底发出了什么。协议文档和实测行为对不上的地方，
  实测赢，而且这处冲突要写下来。
- **注释解释的是*为什么*，并且点名它防住的是哪个故障。** 注释绝不复述
  代码。密度照着 `stages/04-the-cache/render.go` 来。
- **一章要报告它测到的东西**，包括测出来的结果打了这一章自己的脸的时候。
  阶段 04 测出短会话里不打缓存标记反而比打了强；阶段 05 测出上下文压缩
  花掉的比省下的多。两章都照实写了。
- **测试只有过了变异测试才算数。** 故意把代码改坏，一次只改一处，
  确认每改一处都有测试挂掉。变异活下来，说明缺了一条测试。而连
  **编译**都过不去的变异什么也证明不了——在相信"抓住了"之前，先确认
  测试二进制真的编译出来了。

## 命令

```sh
go build ./stages/06-the-composer      # 换成任何一个阶段都行
go test ./...                          # 所有阶段
gofmt -l stages/                       # 必须是空的
go vet ./...                           # 必须干净
GOOS=linux go build ./stages/06-the-composer    # 平台相关的文件是真的要编译过
GOOS=darwin go build ./stages/06-the-composer
```

## 运行 Agent

它会照模型说的去执行。工作目录用 `sandbox/`（已被 gitignore），绝不要用
仓库根目录。凭证来自 `.env`，那个文件同样被 gitignore，从不提交：

```sh
set -a && . ./.env && set +a
```
